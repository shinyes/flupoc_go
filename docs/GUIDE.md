# Flupoc-Go ????

**Flupoc-Go** ????? TLS ????????????????????????Flupoc??????????Poculum??????? HTTP ???? Gin/Echo??????????????????? TCP ??????

## 1. ??????

??????????????????

| ?? | ?? | ?? |
| :--- | :--- | :--- |
| **??? (Router)** | `router/` | ??????????????????? HTTP ???? |
| **??? (TCP Layer)** | `tcp_layer/` | ?? TLS ? TCP ?????????????????????? |
| **??? (Protocol)** | `protocol/` | ?????????Head??????Datagram???? |
| **???? (Poculum)** | `poculum/` | ??????????????????????? |
| **??? (Client)** | `client/` | ???????TLS ?????????????? |

---

## 2. ????

### 2.1 ????
- Go 1.25+
- TLS ????????????????

### 2.2 ????????

?????????????

```go
package main

import (
    "log"
    "github.com/cykyes/flupoc-go/router"
    tcplayer "github.com/cykyes/flupoc-go/tcp_layer"
)

func main() {
    // 1. ?????
    r := router.NewRouter()

    // 2. ????
    r.Post("/echo", func(ctx *router.Context) (*router.Response, error) {
        log.Printf("????: %s", string(ctx.RequestBody))
        return router.Bytes(ctx.RequestBody), nil
    })

    // 3. ?? TLS ???
    // ??? server.crt ? server.key
    err := tcplayer.ListenAndServeTLS(
        []string{"127.0.0.1:5128"}, 
        "server.crt", 
        "server.key", 
        r, 
        nil, // ??????
    )
    if err != nil {
        log.Fatal(err)
    }
}
```

### 2.3 ?????

```go
package main

import (
    "fmt"
    "log"
    "github.com/cykyes/flupoc-go/client"
)

func main() {
    // 1. ????? (??????????)
    cli, _ := client.New(client.Options{
        Insecure: true,
    })

    // 2. ????
    resp, err := cli.Post("127.0.0.1:5128", "/echo", []byte("Hello Flupoc"))
    if err != nil {
        log.Fatal(err)
    }

    // 3. ????
    fmt.Printf("????: %d\n", resp.StatusCode)
    fmt.Printf("????: %s\n", resp.Body)
}
```

---

## 3. ??????

### 3.1 ??? (Protocol)

Flupoc ???? TCP????????? **? (Frame)**?

#### ???? (8 ??)
| ?? | ?? | ?? | ?? |
| :--- | :--- | :--- | :--- |
| 0 | 1 | `Protocol` | ??? `0xCF` |
| 1 | 1 | `Type` | ???? |
| 2 | 2 | `ChannelID` | ?? ID (???????????? 1) |
| 4 | 4 | `DataLength` | ?????? (???) |

#### ????
- `MsgPing (0x01)` / `MsgPong (0x02)`: ??????????????
- `MsgRequest (0x03)`: ????????? Poculum ???? Map?
- `MsgResponse (0x04)`: ????????? Poculum ???? Map?

### 3.2 ???? (Poculum)

Poculum ????????????????? `poculum/` ???

- **??**: ????????????
- **????**:
    - ??: `int`, `uint`, `float`, `bool`, `string`, `[]byte`, `nil`
    - ??: `[]any` (??), `map[string]any` (??)

**????**:
```go
import "github.com/cykyes/flupoc-go/poculum"

data := map[string]any{
    "id": 1001,
    "name": "flupoc",
    "tags": []any{"fast", "secure"},
}

// ???
bytes, _ := poculum.DumpPoculum(data)

// ????
obj, _ := poculum.LoadPoculum(bytes)
```

### 3.3 ??? (Router)

`router` ?????? Web ??? API?

#### ????
???? HTTP ???
```go
r.Get("/users/{id}", getUserHandler)
r.Post("/users", createUserHandler)
r.Put("/users/{id}", updateUserHandler)
r.Delete("/users/{id}", deleteUserHandler)
```

#### ????
?? `{param}` ???????
```go
r.Get("/files/{name}", func(ctx *router.Context) (*router.Response, error) {
    fileName := ctx.Param("name")
    return router.Text("File: " + fileName), nil
})
```

#### ??? (Middleware)
???????????????
```go
// ???????
r.Use(func(next router.HandlerFunc) router.HandlerFunc {
    return func(ctx *router.Context) (*router.Response, error) {
        log.Println("Request:", ctx.Path)
        return next(ctx)
    }
})

// ???
api := r.Group("/api")
api.Use(authMiddleware) // ?? /api ???
api.Get("/info", infoHandler)
```

### 3.4 ??? (TCP Layer)

`tcp_layer` ??????????

**????? (`ServerOptions`)**:
- `IdleTimeout`: ????????????????
- `PingInterval`: ??????? Ping ???????????? Pong?

```go
opts := &tcplayer.ServerOptions{
    IdleTimeout:  5 * time.Minute,
    PingInterval: 30 * time.Second,
}
tcplayer.ListenAndServeTLS(addrs, cert, key, router, opts)
```

---

## 4. ?????

?????????????????? `cmd/` ????

### 4.1 ??? (`cmd/main.go`)
?????????????? `/echo` ???

```powershell
# ?????
go run ./cmd --addrs="0.0.0.0:5128" --cert="cert.pem" --key="key.pem"
```

### 4.2 ????? (`cmd/demo_client/main.go`)
?????????????

```powershell
# ???? (??????)
go run ./cmd/demo_client --addr="127.0.0.1:5128" --insecure=true --body="hello"

# ???? (???????)
go run ./cmd/demo_client \
  --addr="127.0.0.1:5128" \
  --ca="ca.pem" \
  --cert="client.crt" \
  --key="client.key" \
  --path="/echo" \
  --body="test data"
```

---

## 5. API ??

### Router Context
| ?? | ?? |
| :--- | :--- |
| `Param(key)` | ?????? (? `/user/{id}`) |
| `Query(key)` | ?????? (? `?page=1`) |
| `RequestBody` | ??????? |

### Router Response ????
| ?? | ?? |
| :--- | :--- |
| `router.OK(body)` | ?? 200 OK (??) |
| `router.Text(str)` | ?????? |
| `router.JSON(obj)` | ?? JSON ?? |
| `router.Bytes(data)` | ??????? |
| `router.Error(code, msg)` | ?????? |

### Client Options
| ?? | ?? |
| :--- | :--- |
| `DialTimeout` | ???? (?? 5s) |
| `ReadTimeout` | ?????? (?? 5s) |
| `WriteTimeout` | ?????? (?? 5s) |
| `Insecure` | ???? TLS ???? |
| `CAFile` | ??? CA ???? |

---

## 6. TCP ????
? flupoc-go ????TCP ??????????????Heartbeat?????????????? TCP Keepalive??????????????????????????????

??????????? Ping?????? Pong?

### ????
1. ????? `PingInterval` ? `IdleTimeout`
2. ??????? `MsgPing (0x01)`
3. ???????? `MsgPong (0x02)`
4. ???????????? IdleTimeout
5. ??????????????????

### ?????? (protocol/service)
- **pingLoop**: ??? `PingInterval` ???????? Ping????????????
- **????**: ?? `SetReadDeadline` ???????????????
- **Ping/Pong ??**: ?????? Ping????? Pong

### ????? (client)
?????????????????? Ping??? Pong ???????????

### ????
- `PingInterval`?????????? `IdleTimeout`??????
- Ping/Pong ?????????

---

## 7. ????

**Q: ????? Poculum?**
A: Poculum ?? Go ?????? JSON ????? Protobuf ????????? Schema???????????

**Q: ??????????**
A: `client.Client` ??????????????????? `MsgPing` ?????????? `MsgPong` ????????????????????

**Q: ??? TLS ????**
A: ??????????Flupoc ?????? TLS ????????????????? `Insecure: true`?
